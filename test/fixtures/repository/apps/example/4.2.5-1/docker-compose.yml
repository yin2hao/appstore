services:
  builder:
    build: .
  application:
    image: example/app:4.2.5
    restart: unless-stopped
    ports:
      - "8080:8080"
  redis:
    image: redis:7.4.0
    volumes:
      - ./data:/data
  postgres:
    image: postgres:17.4
    environment:
      POSTGRES_PASSWORD: example
