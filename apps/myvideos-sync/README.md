## 产品介绍

**MyVideos Sync** 是一款面向 My Videos 的 WebDAV 容灾同步服务，通过 HTTP API 手动触发同步，将主 WebDAV 的新增、变更和删除镜像到容灾 WebDAV。

## 主要功能

- **完整与选择同步**：支持完整镜像同步，也支持按差异文件路径执行选择同步。
- **并发与流量预算**：支持并发扫描/复制参数与流量预算限制，控制同步效率与带宽消耗。
- **状态与任务查询**：提供健康检查、差异查询和任务查询接口，便于客户端展示同步进度。
