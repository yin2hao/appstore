## Introduction

**VideoBackup Next** is a browser-first encrypted backup system for personal media. The backend manages accounts, permissions, metadata, and short-lived signed URLs while clients transfer encrypted media directly to private object storage.

## Features

- **Encrypted Media Backup**: Supports chunked encrypted backups for videos, novels, and comics.
- **Same-origin Web Access**: The Nginx frontend serves the web UI and proxies backend APIs.
